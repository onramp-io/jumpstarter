import type { NextPage } from 'next';
import { Avatar, Box, Text } from 'grommet';
import home from '../../styles/Home.module.css';

const Comment: NextPage = () => {
  return (
    <Box border={{ color: 'black', size: 'xxsmall'}}
    width={'medium'}>
    </Box>
  );
};

export default Comment;
